name: simple_json_mapper
description: >-
  Classes and helper functions that support JSON code generation and serialization via the
  `simple_json` package.
version: 1.1.3
homepage: https://github.com/FlutterDevTools/simple_json

environment:
  sdk: ">=2.10.5 <3.0.0"

